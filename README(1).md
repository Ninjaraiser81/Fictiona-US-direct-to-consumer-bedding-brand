# AsterVale AI Visibility Measurement Case

Start with `Candidate_Assignment_Brief.docx` and `Data_Dictionary_Metric_Specification.xlsx`.

The `data` folder contains the raw inputs. Do not modify those source files. Your code should write all cleaned and derived files to a separate `outputs` folder.

Your submission must be reproducible from a fresh environment using the instructions in your README. Python is mandatory. SQL is required for the supplied SQLite supporting-analytics database.

All brands, platforms, URLs and data in this case are fictional.

